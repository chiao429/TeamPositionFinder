# 系統架構文件

## 1. 系統概覽

```
┌─────────────────────────────────────────────────────────┐
│                     使用者介面層                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │  SearchBar   │  │   TeamInfo   │  │   SeatMap    │ │
│  │  搜尋欄位    │  │  小隊資訊    │  │  座位平面圖  │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│                     應用程式層                          │
│  ┌──────────────────────────────────────────────────┐  │
│  │              app/page.tsx (主頁面)               │  │
│  │  - 狀態管理 (useState)                           │  │
│  │  - 搜尋邏輯                                      │  │
│  │  - 資料過濾                                      │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│                     資料層                              │
│  ┌──────────────────────────────────────────────────┐  │
│  │         data/teams.json (小隊資料)               │  │
│  │  [                                               │  │
│  │    {                                             │  │
│  │      "team_name": "飛鷹隊",                      │  │
│  │      "positions": ["S223", "S224"],              │  │
│  │      "leader": "王小明"                          │  │
│  │    }                                             │  │
│  │  ]                                               │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

---

## 2. 專案結構

```
TeamPositionFinder/
├── app/                          # Next.js App Router
│   ├── layout.tsx               # 根佈局
│   ├── page.tsx                 # 主頁面（搜尋介面）
│   └── globals.css              # 全域樣式
│
├── components/                   # React 組件
│   ├── SearchBar.tsx            # 搜尋欄組件
│   ├── SeatMap.tsx              # 座位圖組件
│   └── TeamInfo.tsx             # 小隊資訊組件
│
├── data/                         # 資料檔案
│   └── teams.json               # 小隊資料（JSON 格式）
│
├── types/                        # TypeScript 型別定義
│   └── team.ts                  # Team 和 SeatPosition 介面
│
├── docs/                         # 文件
│   ├── requirements.md          # 需求規格書
│   ├── ui-design.md             # UI 設計規格
│   ├── deployment-guide.md      # 部署指南
│   └── system-architecture.md   # 系統架構（本文件）
│
├── public/                       # 靜態資源
│   └── (未來可放置圖片等)
│
├── package.json                  # 專案依賴
├── tsconfig.json                # TypeScript 配置
├── tailwind.config.js           # TailwindCSS 配置
├── next.config.js               # Next.js 配置
├── vercel.json                  # Vercel 部署配置
└── README.md                    # 專案說明
```

---

## 3. 技術棧

### 3.1 前端框架
```
┌─────────────────────────────────────┐
│          Next.js 14                 │
│  (React Server Components)          │
│                                     │
│  ┌─────────────────────────────┐   │
│  │      React 18               │   │
│  │  (UI 組件庫)                │   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

### 3.2 開發工具鏈
```
TypeScript  →  類型安全
TailwindCSS →  樣式設計
Lucide      →  圖示庫
ESLint      →  程式碼檢查
```

### 3.3 部署平台
```
Vercel
  ├── 自動建置
  ├── 全球 CDN
  ├── HTTPS
  └── 持續部署
```

---

## 4. 資料流程

### 4.1 搜尋流程

```
使用者輸入
    ↓
┌─────────────────────┐
│  SearchBar 組件     │
│  - 接收輸入         │
│  - 觸發 onSearch    │
└─────────────────────┘
    ↓
┌─────────────────────┐
│  page.tsx           │
│  handleSearch()     │
│  - 讀取 teams.json  │
│  - 模糊搜尋比對     │
│  - 更新狀態         │
└─────────────────────┘
    ↓
┌─────────────────────┐     ┌─────────────────────┐
│  TeamInfo 組件      │     │  SeatMap 組件       │
│  - 顯示小隊資訊     │     │  - 高亮座位         │
│  - 隊長、位置       │     │  - 動畫效果         │
└─────────────────────┘     └─────────────────────┘
```

### 4.2 狀態管理

```typescript
// 主頁面狀態
const [selectedTeam, setSelectedTeam] = useState<Team | null>(null);
const [highlightedSeats, setHighlightedSeats] = useState<string[]>([]);
const [notFound, setNotFound] = useState(false);

// 狀態流轉
搜尋 → 找到 → selectedTeam = team, highlightedSeats = positions
     → 未找到 → notFound = true, 清空其他狀態
```

---

## 5. 組件架構

### 5.1 組件層次結構

```
app/page.tsx (主頁面)
│
├── SearchBar
│   └── Props: { onSearch: (query: string) => void }
│
├── TeamInfo
│   └── Props: { team: Team | null, notFound: boolean }
│
└── SeatMap
    └── Props: { highlightedSeats: string[] }
```

### 5.2 組件職責

| 組件 | 職責 | 輸入 | 輸出 |
|------|------|------|------|
| **SearchBar** | 接收使用者輸入 | onSearch 回調 | 搜尋查詢字串 |
| **TeamInfo** | 顯示小隊資訊 | team 物件, notFound 旗標 | 視覺化資訊卡片 |
| **SeatMap** | 渲染座位圖 | highlightedSeats 陣列 | 互動式座位圖 |
| **page.tsx** | 協調所有組件 | 使用者操作 | 整合介面 |

---

## 6. 資料模型

### 6.1 Team 介面

```typescript
interface Team {
  team_name: string;      // 小隊名稱
  positions: string[];    // 座位編號陣列
  leader: string;         // 隊長姓名
}
```

**範例**:
```json
{
  "team_name": "飛鷹隊",
  "positions": ["S223", "S224"],
  "leader": "王小明"
}
```

### 6.2 SeatPosition 介面

```typescript
interface SeatPosition {
  id: string;       // 座位唯一 ID (例如: "S223")
  row: number;      // 排數 (0-based)
  col: number;      // 列數 (0-based)
  label: string;    // 顯示標籤 (例如: "S223")
}
```

**範例**:
```typescript
{
  id: 'S223',
  row: 3,
  col: 4,
  label: 'S223'
}
```

---

## 7. 核心演算法

### 7.1 搜尋演算法

```typescript
const handleSearch = (query: string) => {
  // 1. 驗證輸入
  if (!query.trim()) {
    // 清空狀態
    return;
  }

  // 2. 模糊搜尋（不區分大小寫）
  const team = teamsData.find(
    (t) => t.team_name.toLowerCase().includes(query.toLowerCase())
  );

  // 3. 更新狀態
  if (team) {
    setSelectedTeam(team);
    setHighlightedSeats(team.positions);
    setNotFound(false);
  } else {
    setSelectedTeam(null);
    setHighlightedSeats([]);
    setNotFound(true);
  }
};
```

**時間複雜度**: O(n) - n 為小隊數量  
**空間複雜度**: O(1)

### 7.2 座位高亮邏輯

```typescript
const isHighlighted = (seatId: string) => 
  highlightedSeats.includes(seatId);

// 在渲染時應用
className={`
  ${isHighlighted(seat.id) 
    ? 'bg-blue-500 text-white scale-110 animate-pulse' 
    : 'bg-gray-50 text-gray-700'
  }
`}
```

---

## 8. 效能優化策略

### 8.1 客戶端優化

```
1. React.memo() - 避免不必要的重新渲染
2. useMemo() - 快取計算結果
3. useCallback() - 快取回調函數
4. 虛擬化 - 大量座位時使用（未來擴展）
```

### 8.2 建置優化

```
Next.js 自動優化:
├── 程式碼分割 (Code Splitting)
├── 樹搖優化 (Tree Shaking)
├── 壓縮與最小化
└── 靜態生成 (Static Generation)
```

### 8.3 載入優化

```
1. 預載入關鍵資源
2. 延遲載入非關鍵組件
3. 圖片優化（Next.js Image）
4. 字型優化（next/font）
```

---

## 9. 安全性考量

### 9.1 輸入驗證

```typescript
// 防止 XSS 攻擊
const sanitizeInput = (input: string) => {
  return input.trim().slice(0, 50); // 限制長度
};

// React 自動轉義輸出
<div>{team.team_name}</div> // 安全
```

### 9.2 資料驗證

```typescript
// 驗證 JSON 資料格式
const isValidTeam = (team: any): team is Team => {
  return (
    typeof team.team_name === 'string' &&
    Array.isArray(team.positions) &&
    typeof team.leader === 'string'
  );
};
```

---

## 10. 擴展性設計

### 10.1 水平擴展

```
目前架構支援:
├── 增加小隊數量（只需編輯 JSON）
├── 增加座位數量（修改 SeatMap 配置）
├── 多語言支援（i18n 整合）
└── 主題切換（Tailwind 主題）
```

### 10.2 垂直擴展

```
未來可整合:
├── 資料庫（PostgreSQL, MongoDB）
├── API 層（RESTful 或 GraphQL）
├── 認證系統（NextAuth.js）
├── 管理後台（CRUD 操作）
└── 即時更新（WebSocket）
```

---

## 11. 部署架構

### 11.1 Vercel 部署流程

```
GitHub Repository
    ↓ (git push)
┌─────────────────────┐
│  Vercel Platform    │
│  ┌───────────────┐  │
│  │  Build Phase  │  │
│  │  - npm install│  │
│  │  - npm build  │  │
│  └───────────────┘  │
│         ↓           │
│  ┌───────────────┐  │
│  │  Deploy Phase │  │
│  │  - 靜態資源   │  │
│  │  - Edge 函數  │  │
│  └───────────────┘  │
└─────────────────────┘
    ↓
┌─────────────────────┐
│   Global CDN        │
│  (全球節點分發)     │
└─────────────────────┘
    ↓
使用者訪問
```

### 11.2 CDN 分發

```
使用者請求
    ↓
最近的 Edge 節點
    ↓
快取檢查
    ├─ 命中 → 直接返回
    └─ 未命中 → 回源 → 快取 → 返回
```

---

## 12. 監控與日誌

### 12.1 Vercel Analytics

```
追蹤指標:
├── 頁面瀏覽量 (Page Views)
├── 獨立訪客 (Unique Visitors)
├── 載入時間 (Load Time)
└── 核心網頁指標 (Core Web Vitals)
```

### 12.2 錯誤追蹤

```
客戶端錯誤:
└── React Error Boundaries

伺服器端錯誤:
└── Vercel 函數日誌
```

---

## 13. 開發工作流程

### 13.1 本地開發

```bash
# 1. 安裝依賴
npm install

# 2. 啟動開發伺服器
npm run dev

# 3. 開啟瀏覽器
http://localhost:3000

# 4. 即時熱重載（Hot Reload）
# 修改程式碼 → 自動重新載入
```

### 13.2 建置與測試

```bash
# 建置生產版本
npm run build

# 啟動生產伺服器
npm start

# 程式碼檢查
npm run lint
```

### 13.3 部署流程

```bash
# Git 工作流程
git add .
git commit -m "功能描述"
git push origin main

# Vercel 自動觸發部署
# 約 1-2 分鐘後完成
```

---

## 14. 系統限制

### 14.1 當前限制

| 項目 | 限制 | 原因 |
|------|------|------|
| 小隊數量 | 建議 < 1000 | 客戶端搜尋效能 |
| 座位數量 | 建議 < 500 | 渲染效能 |
| 搜尋回應 | < 500ms | 使用者體驗 |
| 檔案大小 | teams.json < 1MB | 載入速度 |

### 14.2 解決方案

```
大量資料處理:
├── 實作分頁
├── 虛擬滾動
├── 伺服器端搜尋
└── 資料庫整合
```

---

## 15. 版本控制策略

### 15.1 Git 分支策略

```
main (生產環境)
  ├── develop (開發環境)
  │   ├── feature/search-enhancement
  │   ├── feature/new-ui
  │   └── bugfix/seat-highlight
  └── hotfix/critical-bug
```

### 15.2 版本號規則

```
語義化版本 (Semantic Versioning)
主版本.次版本.修訂號
例如: 1.2.3

1.x.x - 重大變更（不向後相容）
x.2.x - 新功能（向後相容）
x.x.3 - 錯誤修復
```

---

## 16. 測試策略（未來擴展）

### 16.1 測試金字塔

```
        ┌─────────┐
        │  E2E    │  端對端測試
        ├─────────┤
        │ 整合測試 │  組件整合
        ├─────────┤
        │ 單元測試 │  函數邏輯
        └─────────┘
```

### 16.2 建議測試工具

```
單元測試: Jest + React Testing Library
整合測試: Playwright
視覺測試: Chromatic
效能測試: Lighthouse CI
```

---

## 17. 文件維護

### 17.1 文件結構

```
docs/
├── requirements.md          # 需求規格（產品經理維護）
├── ui-design.md            # UI 設計（設計師維護）
├── system-architecture.md  # 系統架構（開發者維護）
└── deployment-guide.md     # 部署指南（DevOps 維護）
```

### 17.2 更新頻率

- 需求變更 → 立即更新文件
- 新功能開發 → 同步更新架構圖
- 部署流程變更 → 更新部署指南

---

## 18. 總結

### 18.1 系統優勢

✅ **簡單易用**: 直觀的搜尋介面  
✅ **效能優異**: 快速回應，流暢動畫  
✅ **易於維護**: 模組化設計，清晰架構  
✅ **可擴展性**: 支援未來功能擴展  
✅ **部署便捷**: 一鍵部署到 Vercel  

### 18.2 技術亮點

- 🎨 現代化 UI 設計
- ⚡ Next.js 14 最新特性
- 🔍 即時搜尋體驗
- 📱 完整響應式支援
- 🚀 全球 CDN 加速

---

**文件版本**: 1.0  
**最後更新**: 2024  
**維護者**: 開發團隊
